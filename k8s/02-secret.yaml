# NOTE: This file is checked in with PLACEHOLDER values for local learning
# use only. In a real cluster, do NOT commit real secrets to git — generate
# this with `kubectl create secret generic ...` or a secrets manager
# (Sealed Secrets, External Secrets Operator, Vault, etc.) instead.
#
# Values below are base64-encoded. To (re)generate:
#   echo -n 'your-secret-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: kartify-backend-secret
  namespace: kartify
type: Opaque
data:
  # "change_this_to_a_long_random_secret_please" base64-encoded
  JWT_SECRET: Y2hhbmdlX3RoaXNfdG9fYV9sb25nX3JhbmRvbV9zZWNyZXRfcGxlYXNl
