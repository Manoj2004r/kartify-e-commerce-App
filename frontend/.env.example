# Used only in local `npm run dev` (Vite). In Docker/K8s builds this is
# baked in at build time via --build-arg VITE_API_URL, and nginx proxies
# /api to the backend service instead.
VITE_API_URL=http://localhost:5000/api
